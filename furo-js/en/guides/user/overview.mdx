---
title: 'Overview'
---

The User object contains information about a single user in the application.<br/>
Each user has a unique identifier (uid), as well as data such as email address and username. The User object does not contain methods, only data.

### User Data Properties

Properties of the User object can be found [here](/api-reference/data/user).

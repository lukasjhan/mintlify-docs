---
title: 'Get User'
description: 'How to Retrieve the User Object'
---

Accessing the `user` property under the `furo` object allows you to obtain the object of the currently logged-in user.
If not logged in, `furo.user` will return `null`.

### Usage

```js
const currentUser = furo.user;
```
